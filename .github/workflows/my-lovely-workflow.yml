name: Backup Project 
on: [push]

jobs:
  Backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout to Main
        run: |
          git clone https://github.com/JihedTR/devops-technical-test-data.git
          cd devops-technical-test-data
          git config --local user.email "jihed.trifi@gmail.com"
          git config --local user.name "JihedTR"
          git branch -a
       
      - name: test branch
        run: |
          branch_name=$(date +'%d-%m-%Y')-test
          git branch -a
          if ! ( git ls-remote --head | grep -qi $branch_name ) ; then
            git checkout -b $branch_name
            git push --set-upstream origin $branch_name
            echo "La branche est creer"
          fi

