name: Backup Project 
on:
  push:
    branches: "main"

jobs:
  Backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout to Main
        run: |
          echo "Checkout to main"
          git checkout main
          echo "Exit Checkout main"


